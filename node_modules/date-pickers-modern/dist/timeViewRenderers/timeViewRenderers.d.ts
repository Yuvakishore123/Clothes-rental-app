/// <reference types="react" />
import { TimeClockProps } from '../TimeClock';
import { DateOrTimeView } from '../internals';
export interface TimeViewRendererProps<TDate, TView extends DateOrTimeView> extends Omit<TimeClockProps<TDate>, 'views' | 'openTo' | 'view' | 'onViewChange'> {
    view: TView;
    onViewChange?: (view: TView) => void;
    views: readonly TView[];
}
export declare const renderTimeViewClock: <TDate extends unknown>(props: TimeViewRendererProps<TDate, any>) => JSX.Element;
