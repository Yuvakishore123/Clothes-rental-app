import * as React from 'react';
import { SlotComponentProps } from '@mui/base/utils';
import { ExportedCalendarHeaderProps, DayCalendarProps, PickersCalendarHeaderSlotsComponent, PickersCalendarHeaderSlotsComponentsProps, DayValidationProps, DayCalendarSlotsComponent, DayCalendarSlotsComponentsProps, DateView, ExportedUseViewsOptions } from '../internals';
import { DateRange } from '../internal/models/range';
import { DateRangePickerDay, DateRangePickerDayProps } from '../DateRangePickerDay';
import { SlideDirection } from "../DateCalendar/PickersSlideTransition";
export interface DateRangePickerViewMobileSlotsComponent<TDate> extends PickersCalendarHeaderSlotsComponent, Omit<DayCalendarSlotsComponent<TDate>, 'Day'> {
    /**
     * Custom component for day in range pickers.
     * Check the [DateRangePickersDay](https://mui.com/x/api/date-pickers/date-range-picker-day/) component.
     * @default DateRangePickersDay
     */
    Day?: React.ElementType<DateRangePickerDayProps<TDate>>;
}
export interface DateRangePickerViewMobileSlotsComponentsProps<TDate> extends PickersCalendarHeaderSlotsComponentsProps<TDate>, Omit<DayCalendarSlotsComponentsProps<TDate>, 'day'> {
    day?: SlotComponentProps<typeof DateRangePickerDay, {}, DayCalendarProps<TDate> & {
        day: TDate;
    }>;
}
interface ChangeMonthPayload<TDate> {
    direction: SlideDirection;
    newMonth: TDate;
}
interface DesktopDateRangeCalendarProps<TDate> extends Omit<DayCalendarProps<TDate>, 'selectedDays' | 'onFocusedDayChange' | 'classes' | 'components' | 'componentsProps'>, DayValidationProps<TDate>, ExportedCalendarHeaderProps<TDate>, Pick<ExportedUseViewsOptions<DateView>, 'view' | 'views' | 'openTo' | 'onViewChange' | 'focusedView'> {
    /**
     * Overrideable components.
     * @default {}
     */
    components?: DateRangePickerViewMobileSlotsComponent<TDate>;
    /**
     * The props used for each component slot.
     * @default {}
     */
    componentsProps?: DateRangePickerViewMobileSlotsComponentsProps<TDate>;
    value: DateRange<TDate>;
    changeMonth: (date: TDate) => void;
    onYearChange?: (date: TDate) => void;
    isDateDisabled?: (day: (TDate | null)) => boolean;
    handleChangeMonth?: (payload: ChangeMonthPayload<TDate>) => void;
    views: DateView[];
    /**
     * If `true`, today's date is rendering without highlighting with circle.
     * @default false
     */
    disableHighlightToday?: boolean;
    /**
     * Make picker read only.
     * @default false
     */
    readOnly?: boolean;
    changeFocusedDay: (newFocusedDate: (TDate | null), withoutMonthSwitchingAnimation?: (boolean | undefined)) => void;
    /**
     * Callback firing on month change @DateIOType.
     * @template TDate
     * @param {TDate} month The new month.
     * @returns {void|Promise} -
     */
    onMonthChange?: (month: TDate) => void | Promise<void>;
    onFocusedViewChangedIn?: (view: DateView, hasFocus: boolean) => void;
    setCurrentView: (newValue: (DateView | ((prevValue: DateView) => DateView))) => void;
}
/**
 * @ignore - internal component.
 */
export declare function DateRangePickerViewMobile<TDate>(props: DesktopDateRangeCalendarProps<TDate>): JSX.Element;
export {};
